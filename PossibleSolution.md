您遇到的问题在于 `inferStrokeType` 函数目前只依赖**局部角度变化**来检测关键点，再对每个子段进行类型判断，这容易丢失笔画整体的形状信息，导致分割不准确和误判。针对这类问题，我提出以下**综合改进方案**，涵盖关键点检测、子笔画特征提取和类型判定三个层面。

---

## 1. 关键点检测的改进

### 问题
当前使用相邻点之间的瞬时角度变化，容易受噪声和采样密度影响，产生过多的虚假关键点，或遗漏平滑转折。

### 改进方向
- **平滑角度序列**：对角度变化曲线进行高斯滤波或移动平均，消除局部毛刺。
- **基于累积角度变化的分段**：不依赖单个点，而是当**累积方向变化超过阈值**时才确定一个关键点，这样可以保留整体趋势。
- **采用 Douglas-Peucker 算法**：将笔画简化为少量控制点，这些控制点天然对应笔画中的拐点，且对噪声不敏感。

### 示例思路
```cpp
// 1. 计算每个点的累积方向角（平滑后）
vector<double> smoothTheta = ...; // 可选：用 Savitzky-Golay 滤波

// 2. 累积角度变化，当超过阈值时切分
vector<size_t> keyPoints = {0};
double cumAngle = 0.0;
for (size_t i = 1; i < n; ++i) {
    double delta = fmod(smoothTheta[i] - smoothTheta[i-1] + M_PI, 2*M_PI) - M_PI;
    cumAngle += fabs(delta);
    if (cumAngle > ANGLE_THRESH) {
        keyPoints.push_back(i);
        cumAngle = 0.0;
    }
}
keyPoints.push_back(n-1);
```

---

## 2. 子笔画特征的全面提取

### 问题
仅用首尾点连线方向判断子笔画类型（横/竖/撇/捺）过于简单，丢失了中间段的曲率和长度信息。

### 改进方向
为每个子段计算以下特征，用于分类：
- **整体方向**：起点到终点的向量角。
- **长度**：子段的总弧长。
- **宽度/高度比**：在首尾连线方向上的投影长度与垂直方向投影长度之比。
- **曲率特征**：子段的平均曲率、最大曲率位置。
- **形状矩**：如偏心度、紧凑度。
- **与标准模板的匹配度**：可以预存几种典型笔画模板（贝塞尔曲线），计算子段与模板的 Hausdorff 距离。

### 示例：方向与弯折度
```cpp
double totalLen = ...;          // 子段弧长
double chordLen = hypot(dx, dy); // 首尾直线距离
double bend = totalLen / chordLen; // 弯曲度，1.0为直线
```

---

## 3. 子笔画类型的判定规则优化

### 现有规则的问题
- “横”和“提”的区分仅靠终点 y 是否小于起点 y，但“提”常伴随弯曲。
- “钩”的检测依赖于最后一段短且方向突变，但可能误判。

### 改进建议
- **横 (h)**：整体方向接近水平（|θ| < 30°），弯曲度接近 1，且首尾 y 坐标差小于一定阈值。
- **竖 (sh)**：整体方向接近垂直（|θ - 90°| < 30°），弯曲度接近 1。
- **撇 (p)**：整体方向左上→右下（θ 在 90°~180° 之间），弯曲度大于 1.2，且曲率主要集中在前半段。
- **捺 (n)**：整体方向左下→右上（θ 在 -90°~0° 之间），弯曲度大于 1.2，曲率集中在中后段。
- **提 (t)**：整体方向左下→右上（θ 在 -45°~0° 之间），弯曲度接近 1，但长度较短且尾部上扬。
- **钩 (g)**：末尾一段长度小于总长 1/5，且该段与上一段方向夹角 > 60°，同时整段弯曲度 > 1.5。
- **点 (d)**：总长 < 10（根据数据缩放），或宽高比接近 1。

### 引入决策树或简单评分
为每种类型设计一个评分函数，取最高分，而不是简单的 if-else。

---

## 4. 考虑笔画上下文

### 问题
孤立看一个子笔画可能不准确，但在汉字中，不同笔画的组合有规律（如“竖钩”往往是一个整体）。

### 改进方向
- 将整个笔画视为一个序列，使用**隐马尔可夫模型 (HMM)** 或**条件随机场 (CRF)** 来标注各段类型，利用相邻子笔画间的转移概率（例如“横”后很少直接跟“捺”）。
- 若不想引入机器学习，可加入启发式规则：
  - 如果子段长度过短（< 10），且前后类型为“撇”和“捺”，则可能应合并为“撇折”等。
  - 预先定义常见笔画组合（如“横折钩”、“竖弯钩”）作为整体识别，而非拆成多个子笔画。

---

## 5. 整体笔画类型后处理

### 问题
即使子笔画识别准确，最终的汉字笔画序列也可能有误。

### 改进
- 对每个子笔画识别结果进行平滑：例如，连续出现的“横”可合并为一条长横。
- 利用汉字结构知识：某些子笔画组合在真实汉字中很少出现，可作为纠错依据。

---

## 6. 代码层面的实现建议

1. **重写 `inferStrokeType`**：
   - 先使用平滑和累积角度方法提取关键点。
   - 对每个子段调用一个独立的子函数 `identifySubStrokeType(subPoints)`，该函数计算多个特征并返回类型。
   - 最后对子类型序列进行后处理（合并、纠正）。

2. **添加特征计算函数**：
   ```cpp
   struct SubStrokeFeatures {
       double totalLen;
       double chordLen;
       double bend;
       double dirAngle;        // 首尾方向角（弧度）
       double maxCurvature;    // 最大曲率
       double avgCurvature;
       double width;           // 在方向上的投影宽度
       double height;          // 垂直方向投影高度
   };
   SubStrokeFeatures computeFeatures(const vector<Point>& sub);
   ```

3. **使用贝塞尔拟合曲率**：对于长度较长的子段，拟合三次贝塞尔曲线，通过控制点的位置判断是直还是弯，以及弯曲方向。

---

## 总结

您当前的方法是一个典型的**基于拐点分割的笔画识别**，其局限性在于过度依赖局部角度信息。通过引入**累积角度分割**、**多特征提取**、**规则优化**以及**上下文后处理**，可以显著提高识别准确率。如果数据量足够，也可以考虑训练一个轻量级分类器（如随机森林）来替代手工规则。

建议先实现第1、2、3点，将子笔画识别准确率提升，再逐步添加上下文逻辑。