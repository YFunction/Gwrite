### 大体实现方法

#### 构造函数

    读取配置文件(./config/smooth_config.json)
    option决定使用哪个数据源，若为2，则在构造函数中读取所有数据

#### printGCode

    1. 分割成单字符，对字符按顺序处理
    2. 查找是否存在于map中，不存在则loadCharData
    3. 大小放缩，位置调整
    4. 打印出GCode

    函数原型:void printGCode(string name,double Rate,double x,double y,double z_up,double z_down,int n=1, int m=1,double Size=100.0,bool rowMajor=true,string path="./G.gcode");
    string name:需要书写的字（可以为多个）
    double x,double y:第一个字的相对零点（左下角）
    double z_up,double z_down:起落笔高度，需要同时调节Rotrics Studio中机械臂工作高度，同时，z_up-z_down影响z轴变化幅度
    int n,int m:每行每列写多少个字，最多写n*m个，多余忽略
    double Size:大小(mm)
    bool rowMajor:true列优先，false行优先
    string path:输出文件相对路径

    推荐传参：printGCode(write, 1000.0, -200, 200, 0, -2, 18, 1, 30.0, false)

#### loadCharData

    1. 读取./hanzi_data/*.json，特殊字符单独读取，骨架点按笔画顺序分笔画储存在json文件medians字段，对单个笔画进行处理
    2. 通过几何方法判断壁画类型(inferStrokeType)，将复杂壁画拆解成七种基础笔画：点，横，竖，撇，那，提，钩，并储存子笔画对应点起始位置
    3. 在骨架点之间进行Catmull-Rom样条插值，确保轨迹平滑，并更新子笔画下标(updateSubStrokeIndices)
    4. 笔迹末端延长，以保证在笔尖下压时达到相应的笔迹效果
    5. 增加Z轴变化(applyZProfile)，子笔画不同类型对应不同函数，同时子笔画交界处增加而外的顿挫
    6. 增加回笔(addBackstroke)，使得更符合毛笔写字轨迹
    7. (personalizeStroke)对于每种笔画单独加修正，使其符合毛笔写字规律
    8. 平滑函数(smooth)，删去过于邻近的点，临近点加权平均，去除近似贡献的冗余点。
    9. push至笔画内
